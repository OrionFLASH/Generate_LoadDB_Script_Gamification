# Генератор JavaScript Скриптов для Геймификации

Единая программа для генерации JavaScript скриптов и обработки JSON файлов в Excel. Все функции объединены в одном файле `main.py` с системой выбора операций через переменные.

## Возможности

### 🔧 Генерация скриптов
- ✅ Генерация JavaScript скриптов для различных сервисов
- ✅ Поддержка множественных вариантов (SIGMA, ALPHA)
- ✅ Автоматическое копирование в буфер обмена
- ✅ Настраиваемые источники данных (файлы, переменные)

### 📊 Обработка JSON в Excel
- ✅ Конвертация JSON файлов LeadersForAdmin в Excel
- ✅ Создание структурированных Excel файлов с несколькими листами
- ✅ Автоматическое форматирование и стилизация
- ✅ Статистика и сводные данные
- ✅ Обработка европейского формата чисел

## Структура проекта

```
Generate_LoadDB_Script_Gamification/
├── main.py                    # 🎯 Основная программа (все функции)
├── README.md                  # 📖 Документация
├── requirements.txt           # 📦 Python зависимости
├── environment.yml           # 🐍 Conda окружение (глобальное)
├── setup_environment.sh      # ⚙️ Скрипт настройки глобального окружения
├── activate_local.sh         # 🔧 Скрипт активации локального окружения
├── .gitignore                # 🚫 Git настройки исключений
├── .cursorrules              # ⚙️ Настройки Cursor IDE
├── local_env/                # 🏠 Локальное окружение (не в git)
├── WORK/                     # 📁 Рабочая директория (создается автоматически)
│   ├── LOGS/                # 📝 Логи программы
│   ├── INPUT/               # 📁 Входные файлы
│   │   ├── *.csv           # Данные для генерации скриптов
│   │   └── *.json          # JSON файлы для обработки
│   ├── OUTPUT/              # 📁 Выходные файлы
│   │   └── *.xlsx          # Созданные Excel файлы
│   ├── SCRIPT/              # 📜 Сгенерированные скрипты
│   ├── CONFIG/              # ⚙️ Конфигурационные файлы
│   └── JSON/                # 📄 JSON файлы для обработки
```

## Настройка операций

### Основные настройки в `main.py`:

```python
# Выбор активных скриптов для генерации
ACTIVE_SCRIPTS = [
    "leaders_for_admin",  # Информация по участникам турнира
    # "reward",             # Информация о наградах сотрудников
    # "profile",            # Профили сотрудников
    # "news_details",       # Детальная карточка новости
    # "address_book_tn",    # Карточка сотрудника по табельному номеру
    # "address_book_dev",   # Карточка подразделения
    # "orders",             # Список сотрудников с преференциями
    # "news_list",          # Список новостей
    # "rating_list"         # Рейтинг участников
]

# Базовая директория проекта
BASE_DIR = r"/Users/orionflash/Desktop/MyProject/Gen_Load_Game_Script/WORK"

# Поддиректории
SUBDIRECTORIES = {
    "LOGS": "LOGS",           # Папка для логов
    "INPUT": "INPUT",         # Папка для входных файлов
    "OUTPUT": "OUTPUT",       # Папка для выходных файлов
    "SCRIPT": "SCRIPT",       # Папка для сгенерированных скриптов
    "CONFIG": "CONFIG",       # Папка для конфигурационных файлов
    "JSON": "JSON"            # Папка для JSON файлов
}
```

## Начальные настройки и константы

### Настройки логирования

- **LOG_LEVEL**: Уровень логирования (`"INFO"` или `"DEBUG"`)
  - `INFO`: Базовые сообщения о старте, остановке и общей сводке
  - `DEBUG`: Подробная информация о выполнении каждой функции

- **LOG_FILENAME_BASE**: Базовое имя файла лога без расширения
  ```python
  LOG_FILENAME_BASE = "LOG"
  ```

### Формат имени файла лога

Файл лога создается по шаблону:
```
{LOG_FILENAME_BASE}_{LOG_LEVEL}_{YYYY-MM-DD}.log
```

Пример: `LOG_DEBUG_2024-01-15.log`

### Настройки каталогов

- **BASE_DIR**: Базовая директория проекта
  ```python
  BASE_DIR = r"/Users/orionflash/Desktop/MyProject/Gen_Load_Game_Script/WORK"
  ```

- **SUBDIRECTORIES**: Словарь с названиями поддиректорий
  ```python
  SUBDIRECTORIES = {
      "LOGS": "LOGS",           # Папка для логов
      "INPUT": "INPUT",         # Папка для входных файлов
      "OUTPUT": "OUTPUT",       # Папка для выходных файлов
      "SCRIPT": "SCRIPT",       # Папка для сгенерированных скриптов
      "CONFIG": "CONFIG",       # Папка для конфигурационных файлов
      "JSON": "JSON"            # Папка для JSON файлов
  }
  ```

### Настройки обработки данных

- **DATA_SOURCE**: Источник данных (`"file"`, `"variable"` или `"external_file"`)
- **INPUT_FORMAT**: Формат входного файла (`"TXT"`, `"CSV"` или `"JSON"`)
- **INPUT_FILENAME**: Имя входного файла (без расширения)
- **INPUT_FILE_EXTENSION**: Расширение входного файла

### Расширения файлов

```python
FILE_EXTENSIONS = {
    "CSV": ".csv",    # Ключ: формат CSV файлов
    "TXT": ".txt",    # Ключ: формат текстовых файлов  
    "JSON": ".json",  # Ключ: формат JSON файлов
    "EXCEL": ".xlsx"  # Ключ: формат Excel файлов
}
```

### Настройки для TXT файлов

- **TXT_DELIMITERS**: Массив разделителей для TXT файлов
- Поддерживаемые разделители: `,`, `;`, `\t`, ` `, `\n`, `\r\n`, `|`, `:`, `.`, `!`, `?`, `@`, `#`, `$`, `%`, `^`, `&`, `*`, `(`, `)`, `[`, `]`, `{`, `}`, `<`, `>`, `/`, `\\`, `=`, `+`, `~`, `` ` ``, `'`, `"`
- Программа автоматически обрабатывает все разделители из массива, кроме "_" и "-"

### Настройки для CSV файлов

- **CSV_DELIMITER**: Разделитель колонок в CSV (по умолчанию `;`)
- **CSV_ENCODING**: Кодировка CSV файла (`"utf-8"`)
- **CSV_COLUMN_NAME**: Название столбца для извлечения данных

### Тексты для логирования

- **LOG_MESSAGES**: Словарь с шаблонами сообщений для логирования
- Все тексты используют форматирование с переменными: `{time}`, `{file_path}`, `{error}` и т.д.
- Примеры:
  ```python
  "start": "=== Старт работы программы: {time} ===",
  "reading_file": "Загрузка файла: {file_path}",
  "read_ok": "Файл успешно загружен: {file_path}, строк: {rows}",
  "func_start": "[START] {func} {params}",
  "func_end": "[END] {func} (время: {time:.3f}s)"
  ```

## Использование

### 1. Запуск программы
```bash
python main.py
```

### 2. Настройка операций

#### Только генерация скриптов:
```python
# В конфигурации скрипта установите:
"active_operations": "scripts_only"
```

#### Только обработка JSON:
```python
# В конфигурации скрипта установите:
"active_operations": "json_only"
```

#### Обе операции:
```python
# В конфигурации скрипта установите:
"active_operations": "both"
```

### 3. Выбор скриптов для генерации

Раскомментируйте нужные скрипты в `ACTIVE_SCRIPTS`:
```python
ACTIVE_SCRIPTS = [
    "leaders_for_admin",  # ✅ Активен
    "reward",             # ✅ Активен
    # "profile",          # ❌ Отключен
    # "news_details",     # ❌ Отключен
]
```

### 4. Настройка обработки JSON

Измените параметры в конфигурации скрипта:
```python
"json_file": "leadersForAdmin_SIGMA_20250726-192035",  # Имя JSON файла без расширения
"excel_file": "LeadersForAdmin_Excel",  # Имя Excel файла без расширения
"excel_freeze_cell": "B2"  # Ячейка для закрепления в Excel (B2 = первая строка и первая колонка)
```

### 5. Система именования Excel файлов

Excel файлы создаются с уникальными именами по шаблону:
```
{excel_file}_{SIGMA/ALPHA}_{YYYY-MM-DD-HH-MM-SS}.xlsx
```

Пример: `LeadersForAdmin_Excel_SIGMA_2025-07-27-00-34-05.xlsx`

- **excel_file** - имя из конфигурации скрипта
- **SIGMA/ALPHA** - выбранный вариант окружения
- **YYYY-MM-DD-HH-MM-SS** - временная метка создания файла

### 6. Система закрепления ячеек в Excel

Закрепление строк и столбцов настраивается через параметр `excel_freeze_cell`:

- **"B2"** - закрепляет первую строку и первую колонку (A1 остается видимой)
- **"A2"** - закрепляет только первую строку
- **"B1"** - закрепляет только первую колонку
- **"C3"** - закрепляет первые две строки и первые две колонки

Формат: `{колонка}{строка}` (например, "B2", "C3", "A2")

## Конфигурация скриптов

### FUNCTION_CONFIGS

Каждый скрипт имеет свою конфигурацию в словаре `FUNCTION_CONFIGS`:

```python
FUNCTION_CONFIGS = {
    "leaders_for_admin": {  # Ключ: конфигурация для скрипта LeadersForAdmin
        "name": "LeadersForAdmin",  # Ключ: название скрипта для отображения
        "description": "Информация по загруженным в турнир данным об участниках",  # Ключ: описание назначения скрипта
        "active_operations": "both",  # Ключ: активные операции ("scripts_only", "json_only", "both")
        "variants": {  # Ключ: варианты конфигурации (SIGMA/ALPHA)
            "sigma": {  # Ключ: вариант SIGMA (продакшн окружение)
                "name": "LeadersForAdmin (SIGMA)",  # Ключ: название варианта
                "description": "Информация по загруженным в турнир данным об участниках - SIGMA",  # Ключ: описание варианта
                "domain": "https://salesheroes.sberbank.ru",  # Ключ: домен для SIGMA
                "params": {  # Ключ: параметры API запросов
                    "api_path": "/bo/rmkib.gamification/api/v1/tournaments/",  # Ключ: путь к API
                    "service": "leadersForAdmin",  # Ключ: название сервиса
                    "page_param": "pageNum=1"  # Ключ: параметр пагинации
                }
            },
            "alpha": {  # Ключ: вариант ALPHA (тестовое окружение)
                "name": "LeadersForAdmin (ALPHA)",  # Ключ: название варианта
                "description": "Информация по загруженным в турнир данным об участниках - ALPHA",  # Ключ: описание варианта
                "domain": "https://efs-our-business-prom.omega.sbrf.ru",  # Ключ: домен для ALPHA
                "params": {  # Ключ: параметры API запросов
                    "api_path": "/bo/rmkib.gamification/api/v1/tournaments/",  # Ключ: путь к API
                    "service": "leadersForAdmin",  # Ключ: название сервиса
                    "page_param": "pageNum=1"  # Ключ: параметр пагинации
                }
            }
        },
        "selected_variant": "sigma",  # Ключ: выбранный вариант (sigma/alpha)
        "data_source": "external_file",  # Ключ: источник данных (file/variable/external_file)
        "input_format": "CSV",  # Ключ: формат входного файла
        "csv_column": "TOURNAMENT_CODE",  # Ключ: название столбца для извлечения данных
        "csv_delimiter": ";",  # Ключ: разделитель в CSV файле
        "csv_encoding": "utf-8",  # Ключ: кодировка CSV файла
        "input_file": "TOURNAMENT-SCHEDULE (PROM) 2025-07-25 v6",  # Ключ: имя входного файла (без расширения)
        "json_file": "leadersForAdmin_SIGMA_20250726-192035",  # Ключ: имя JSON файла для обработки (без расширения)
        "excel_file": "LeadersForAdmin_Excel",  # Ключ: имя Excel файла для создания (без расширения)
        "excel_freeze_cell": "B2"  # Ключ: ячейка для закрепления в Excel (B2 = первая строка и первая колонка)
    }
}
```

## Доступные скрипты

### 1. leaders_for_admin
- **Описание**: Информация по загруженным в турнир данным об участниках
- **Поддерживает**: SIGMA и ALPHA варианты
- **Операции**: Генерация скриптов + обработка JSON
- **Источник данных**: CSV файл с колонкой TOURNAMENT_CODE

### 2. reward
- **Описание**: Информация о сотрудниках которые уже получили награды
- **Операции**: Только генерация скриптов
- **Источник данных**: CSV файл с колонкой employee_id

### 3. profile
- **Описание**: Профили сотрудников в героях продаж
- **Операции**: Только генерация скриптов
- **Источник данных**: TXT файл с колонкой profile_id

### 4. news_details
- **Описание**: Детальная карточка новости
- **Операции**: Только генерация скриптов
- **Источник данных**: TXT файл с колонкой news_id

### 5. address_book_tn
- **Описание**: Карточка сотрудника из адресной книги по табельным номерам
- **Операции**: Только генерация скриптов
- **Источник данных**: CSV файл с колонкой employee_number

### 6. address_book_dev
- **Описание**: Карточка подразделения из адресной книги со списком сотрудников
- **Операции**: Только генерация скриптов
- **Источник данных**: CSV файл с колонкой department_id

### 7. orders
- **Описание**: Список сотрудников выбравших преференции
- **Операции**: Только генерация скриптов
- **Источник данных**: CSV файл с колонкой employee_id

### 8. news_list
- **Описание**: Список новостей
- **Операции**: Только генерация скриптов
- **Источник данных**: Переменная (тестовые данные)

### 9. rating_list
- **Описание**: Рейтинг участников по полученным наградам и кристаллам
- **Операции**: Только генерация скриптов
- **Источник данных**: CSV файл с колонкой participant_id

## Структура JSON данных

Программа ожидает JSON файл со следующей структурой:

```json
{
  "tournament_key": [
    {
      "success": true,
      "body": {
        "tournament": {
          "tournamentId": "...",
          "leaders": [
            {
              "employeeNumber": "...",
              "lastName": "...",
              "firstName": "...",
              "indicatorValue": "...",
              "successValue": "...",
              "terDivisionName": "...",
              "employeeStatus": "...",
              "businessBlock": "...",
              "divisionRatings": [
                {
                  "groupCode": "BANK",
                  "groupId": "0",
                  "placeInRating": "406",
                  "ratingCategoryName": "Нужно поднажать"
                },
                {
                  "groupCode": "GOSB",
                  "groupId": "8636",
                  "placeInRating": "2",
                  "ratingCategoryName": "Нужно поднажать"
                },
                {
                  "groupCode": "TB",
                  "groupId": "70",
                  "placeInRating": "25",
                  "ratingCategoryName": "Нужно поднажать"
                }
              ]
            }
          ]
        }
      }
    }
  ]
}
```

### Обработка divisionRatings

Программа автоматически парсит вложенную структуру `divisionRatings` и создает отдельные колонки для каждой категории:

- **BANK_groupId** - ID группы для категории BANK
- **BANK_placeInRating** - место в рейтинге для категории BANK  
- **BANK_ratingCategoryName** - название категории рейтинга для BANK
- **TB_groupId** - ID группы для категории TB
- **TB_placeInRating** - место в рейтинге для категории TB
- **TB_ratingCategoryName** - название категории рейтинга для TB
- **GOSB_groupId** - ID группы для категории GOSB
- **GOSB_placeInRating** - место в рейтинге для категории GOSB
- **GOSB_ratingCategoryName** - название категории рейтинга для GOSB

Категория определяется по полю `groupCode` в каждом элементе массива `divisionRatings`.

## Выходные данные Excel

### Лист DATA
Содержит основные данные участников:
- employeeNumber - номер сотрудника
- lastName, firstName - фамилия и имя
- fullName - полное имя
- indicatorValue - значение показателя
- successValue - целевое значение
- terDivisionName - территориальное подразделение
- employeeStatus - статус сотрудника
- businessBlock - бизнес-блок
- indicatorValue_parsed, successValue_parsed - числовые значения
- **Колонки divisionRatings** - данные по категориям BANK, TB, GOSB:
  - BANK_groupId, TB_groupId, GOSB_groupId - ID групп
  - BANK_placeInRating, TB_placeInRating, GOSB_placeInRating - места в рейтингах
  - BANK_ratingCategoryName, TB_ratingCategoryName, GOSB_ratingCategoryName - названия категорий

### Лист SUMMARY
Сводная статистика:
- Общее количество участников
- Участники с номером сотрудника
- Участники со статусом CONTESTANT
- Среднее/максимальное/минимальное значение показателя

### Лист STATISTICS
Статистика по:
- Территориальным подразделениям
- Бизнес-блокам

## Примеры использования

### Пример 1: Только генерация скрипта LeadersForAdmin
```python
ACTIVE_SCRIPTS = ["leaders_for_admin"]
# В конфигурации: "active_operations": "scripts_only"
```

### Пример 2: Только обработка JSON файлов
```python
ACTIVE_SCRIPTS = ["leaders_for_admin"]
# В конфигурации: "active_operations": "json_only"
```

### Пример 3: Полный цикл (генерация + обработка)
```python
ACTIVE_SCRIPTS = ["leaders_for_admin"]
# В конфигурации: "active_operations": "both"
```

### Пример 4: Конвертация файла LeadersForAdmin
```bash
# Разместите JSON файл в WORK/JSON/ директории
# Настройте "active_operations": "json_only" в конфигурации
python main.py
```

## Полный рабочий процесс

### 1. Генерация JavaScript скриптов

```bash
# Настройте ACTIVE_SCRIPTS в main.py
ACTIVE_SCRIPTS = ["leaders_for_admin", "reward"]

# Запустите генерацию
python main.py
```

### 2. Получение JSON данных

Выполните сгенерированные скрипты в браузере (DevTools Console) для получения JSON ответов.

### 3. Обработка JSON в Excel

```bash
# Разместите JSON файлы в WORK/JSON/ папке
# Настройте "active_operations": "json_only" в конфигурации
python main.py
```

### 4. Результат

Excel файлы с обработанными данными появятся в папке WORK/OUTPUT/.

## Установка зависимостей

```bash
pip install pandas openpyxl pyperclip
```

## Особенности

1. **Единый файл**: Все функции в одном `main.py`
2. **Гибкая настройка**: Выбор операций через конфигурацию скриптов
3. **Автоматизация**: Автоматический поиск и обработка файлов
4. **Форматирование**: Красивое оформление Excel файлов
5. **Логирование**: Подробные логи всех операций
6. **Обработка ошибок**: Корректная обработка различных форматов данных
7. **Автоматическое создание директорий** - программа создает необходимые директории для выходного файла
8. **Обработка европейского формата чисел** - корректная обработка европейского формата чисел (запятая вместо точки)
9. **Автоматическая настройка ширины столбцов** в Excel файлах
10. **Поддержка множественных вариантов** - SIGMA и ALPHA окружения
11. **Автоматическое копирование в буфер обмена** - сгенерированные скрипты автоматически копируются

## Структура SUMMARY

В конце выполнения программа выводит статистику:
- Общее время выполнения
- Количество обработанных действий  
- Время выполнения каждой функции
- Дата и время завершения

Пример:
```
======================================================================
SUMMARY - ИТОГОВАЯ СТАТИСТИКА РАБОТЫ ПРОГРАММЫ
======================================================================
Общее время выполнения: 0.0097 секунд
Обработано действий: 5
Выполнено функций: 4

Время выполнения функций:
  - load_data_from_file: 0.0002 сек
  - generate_script_universal: 0.0091 сек
  - copy_to_clipboard: 0.0088 сек

Программа завершена: 2025-07-26 16:45:58.710
======================================================================
```

## Поддержка

Программа поддерживает:
- ✅ Различные форматы входных данных (CSV, TXT, JSON)
- ✅ Европейский формат чисел (запятая вместо точки)
- ✅ Автоматическое создание директорий
- ✅ Обработка больших файлов
- ✅ Детальное логирование
- ✅ Настраиваемые стили Excel
- ✅ Множественные варианты конфигурации (SIGMA/ALPHA)
- ✅ Автоматическое копирование в буфер обмена
- ✅ Гибкая настройка операций для каждого скрипта
- ✅ Поддержка локального окружения Python
- ✅ Парсинг вложенной структуры divisionRatings
- ✅ Автоматическое закрепление строк в Excel
- ✅ Автофильтр для листа DATA
- ✅ Настраиваемые параметры Excel через конфигурацию

## Автор

OrionFLASH

## Версия

### История версий

**2.4.0** - Версия с системой закрепления ячеек в Excel
- Изменен параметр `excel_freeze_row` на `excel_freeze_cell`
- Теперь используется формат ячейки (например, "B2") вместо номера строки
- B2 закрепляет первую строку и первую колонку
- Добавлены примеры различных вариантов закрепления

**2.3.0** - Версия с улучшенной системой именования Excel файлов
- Добавлен параметр `excel_file` в конфигурацию скрипта
- Добавлено расширение `EXCEL` в `FILE_EXTENSIONS`
- Реализована генерация уникальных имен Excel файлов
- Формат имени: `{excel_file}_{SIGMA/ALPHA}_{YYYY-MM-DD-HH-MM-SS}.xlsx`

**2.2.0** - Версия с поддержкой парсинга divisionRatings и настройками Excel
- Добавлена обработка вложенной структуры `divisionRatings`
- Создание колонок для категорий BANK, TB, GOSB
- Добавлен параметр `excel_freeze_row` для закрепления строк
- Реализован автофильтр для листа DATA
- Обновлена функция `flatten_leader_data` для парсинга divisionRatings

**2.1.0** - Версия с улучшенной конфигурацией и структурой проекта
- Переход на конфигурационный подход через `FUNCTION_CONFIGS`
- Добавлена поддержка `active_operations` для каждого скрипта
- Реализована новая структура директорий (`WORK/`, `LOGS/`, `INPUT/`, `OUTPUT/`, `SCRIPT/`, `CONFIG/`, `JSON/`)
- Добавлена поддержка различных форматов файлов (CSV, TXT, JSON)
- Улучшена система логирования и обработки ошибок

**2.0.0** - Версия с поддержкой JSON в Excel конвертации
- Добавлена функция `convert_json_to_excel` для конвертации JSON в Excel
- Реализована обработка вложенных структур данных
- Добавлено создание дополнительных листов (SUMMARY, STATISTICS)
- Реализовано автоматическое форматирование Excel файлов
- Добавлена поддержка различных источников данных

**1.5.0** - Версия с улучшенной генерацией скриптов
- Добавлена поддержка различных вариантов окружения (SIGMA, ALPHA)
- Реализована универсальная функция `generate_script_universal`
- Добавлена поддержка копирования в буфер обмена
- Улучшена система логирования и измерения времени выполнения

**1.4.0** - Версия с поддержкой CSV файлов
- Добавлена функция `load_data_from_file` для загрузки CSV данных
- Реализована поддержка различных разделителей и кодировок
- Добавлена обработка ошибок при загрузке файлов
- Улучшена система конфигурации скриптов

**1.3.0** - Версия с системой логирования
- Добавлена функция `setup_logging` для настройки логирования
- Реализованы различные уровни логирования (DEBUG, INFO, ERROR)
- Добавлено измерение времени выполнения функций
- Улучшена обработка ошибок и отладка

**1.2.0** - Версия с базовой функциональностью
- Реализована генерация JavaScript скриптов
- Добавлена поддержка различных API endpoints
- Реализована базовая структура проекта
- Добавлена функция `save_script_to_file`

**1.1.0** - Версия с инициализацией проекта
- Создана базовая структура файлов
- Добавлены глобальные настройки и константы
- Реализована основная функция `main()`
- Добавлена поддержка различных форматов файлов

**1.0.0** - Первоначальная версия
- Базовая функциональность генератора скриптов
- Поддержка основных операций с файлами
- Простая система конфигурации 